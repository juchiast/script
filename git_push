#!/usr/bin/perl
my@repo = ('appmenu', 'backup', 'clock', 'dict', 'perl',
           'script', 'termise', 'timeline', 'vntyper', 'mysite');

for (@repo) {
    print "Enter $_\n";
    chdir $_;
    system "git push -u";
    chdir '..';
}
