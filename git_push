#!/usr/bin/perl
my@repo = ('appmenu', 'backup', 'dict', 'perl',
           'script', 'termise', 'timeline', 'vntyper',
           'site', 'sitedb', 'sitejs');

for (@repo) {
if (-d $_) {
    print "Enter $_\n";
    chdir $_;
    system "git push -u";
    chdir '..';
}
}
