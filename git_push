#!/usr/bin/perl
my@repo = ('appmenu', 'backup', 'dict', 'perl',
           'script', 'termise', 'timeline', 'vntyper');

for (@repo) {
if (-d $_) {
    print "Enter $_\n";
    chdir $_;
    system "git push -u";
    chdir '..';
}
}
